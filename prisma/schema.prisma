generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGO_URL")
}

model Session {
  id          String   @id @map("_id")
  shop        String
  state       String
  isOnline    Boolean
  scope       String
  accessToken String
  expires     DateTime?
  createdAt   DateTime @default(now())
}
